// Page-level composition for the PriceNest homepage.
// The current no-build Flask runtime serves `static/js/app.js`; this source tree
// is ready for a future Vite/React build without changing backend APIs.

export { default } from "../App.jsx";
